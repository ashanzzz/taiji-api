{
  "name": "taiji-api",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Private account-scoped Taiji API relay and capability lab",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js",
    "check": "node scripts/check.mjs",
    "pack": "node scripts/build-zip.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
